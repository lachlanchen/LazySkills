interface:
  display_name: "Publish Repo"
  short_description: "Safely publish a local repo to GitHub"
  brand_color: "#0F766E"
  default_prompt: "Use $publish-repo to prepare this local repository, publish it to GitHub, and set homepage, description, and topics."
policy:
  allow_implicit_invocation: true
